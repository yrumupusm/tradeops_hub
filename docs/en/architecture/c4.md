# C4 architecture

[한국어](../../ko/architecture/c4.md) · [English](c4.md)

## System context

```mermaid
flowchart LR
 U["User / owner"] -->|"Collect / search / download"| H["TradeOps Hub"]
 H -->|"Public guidance and DPL/EL downloads"| B["Official BIS website"]
```

## Containers

```mermaid
flowchart LR
 Browser["Browser · Korean UI"] -->|"Same-origin HTTP + session cookie"| Web["Next.js · UI / API proxy"]
 Web --> API["Spring Boot · API / authentication / services"]
 API --> DB[("PostgreSQL · accounts / sessions / audit / collection / search")]
 API --> Files[("Persistent files · BIS originals")]
 API -->|"Official HTTPS only"| BIS["BIS guidance / files"]
 Worker["Same API process · weekly scheduler / worker"] --> API
```

## API components

```mermaid
flowchart TB
 Security["SecurityConfig · sessions / CSRF / forced password change"] --> Controllers["Auth / Account / BIS controllers"]
 Controllers --> Account["AccountService · fixed owner / session revocation"]
 Controllers --> Collection["BisCollectionService · scheduled / manual collection"]
 Controllers --> Search["BisSearchService · pinned versions / history / CSV"]
 Collection --> Discovery["BisDiscovery + BisHttpClient · links / TLS / size / retry"]
 Collection --> Parser["BisParser · raw / normalized values / row issues"]
 Collection --> BR["BisRepository · run lock / atomic publication"]
 Search --> SR["BisSearchRepository · pg_trgm / ranking / duplicate grouping"]
 Account --> AR["AccountRepository"]
 Collection --> Audit["AuditService / AuditRepository"]
 Account --> Audit
 Search --> Audit
 BR --> Audit
```

## Deployment boundary

One Next.js process, **one** Spring Boot process (including scheduler), one PostgreSQL database and an original-file volume. DB transactions protect source pointers; the worker and restart recovery assume a single API instance. Do not scale API replicas without a distributed lease and worker recovery redesign. Production TLS terminates at the trusted ingress; use Secure cookies. API health exposes safe component state only.

Historical V1–V4 tables remain; active collection/search uses V5–V8 tables. Detail and CSV read immutable snapshot rows rather than a mutable current-list table.


Law search uses a separate RAG service. See [law integration](../law-integration.md) for questions, citations, history/diff, authorization and data preservation.

```mermaid
flowchart LR
 Hub["Hub LawController / LawService / LawClient"] --> RAG["Independent RAG API"]
 RAG --> DB[("RAG PostgreSQL")]
 RAG --> Q[("Qdrant")]
 RAG --> Model["Model providers"]
```
