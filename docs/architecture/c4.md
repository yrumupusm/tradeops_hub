# C4 architecture

## System context

```mermaid
flowchart LR
 U["사용자 / 운영 책임자"] -->|"수집·검색·다운로드"| H["TradeOps Hub"]
 H -->|"공개 안내 페이지 및 DPL/EL 다운로드"| B["BIS 공식 웹사이트"]
```

## Containers

```mermaid
flowchart LR
 Browser["브라우저 · 한국어 UI"] -->|"동일 출처 HTTP + 세션 쿠키"| Web["Next.js · 화면 / API proxy"]
 Web --> API["Spring Boot · API / 인증 / 업무 서비스"]
 API --> DB[("PostgreSQL · 계정 / 세션 / 감사 / 수집 / 검색")]
 API --> Files[("영속 파일 저장소 · BIS 원본")]
 API -->|"공식 HTTPS만 허용"| BIS["BIS 안내 / 파일"]
 Worker["같은 API 프로세스 · 주간 스케줄러 / worker"] --> API
```

## API components

```mermaid
flowchart TB
 Security["SecurityConfig · 세션 / CSRF / 강제 비밀번호 변경"] --> Controllers["Auth / Account / BIS controllers"]
 Controllers --> Account["AccountService · 고정 owner 권한 / 세션 폐기"]
 Controllers --> Collection["BisCollectionService · 예약 / 수동 수집"]
 Controllers --> Search["BisSearchService · 버전 고정 / 검색 이력 / CSV"]
 Collection --> Discovery["BisDiscovery + BisHttpClient · 링크 / TLS / 크기 / 재시도"]
 Collection --> Parser["BisParser · 원문 / 정규화 값 / 행 문제"]
 Collection --> BR["BisRepository · 실행 잠금 / 원자적 스냅샷 반영"]
 Search --> SR["BisSearchRepository · pg_trgm / 정렬 / 중복 행 그룹"]
 Account --> AR["AccountRepository"]
 Collection --> Audit["AuditService / AuditRepository"]
 Account --> Audit
 Search --> Audit
 BR --> Audit
```

## Deployment boundary

One Next.js process, **one** Spring Boot process (including scheduler), one PostgreSQL database and an original-file volume. DB transactions protect source pointers; the worker and restart recovery assume a single API instance. Do not scale API replicas without a distributed lease and worker recovery redesign. Production TLS terminates at the trusted ingress; use Secure cookies. API health exposes safe component state only.

Historical V1–V4 tables remain; active collection/search uses V5–V8 tables. Detail and CSV read immutable snapshot rows rather than a mutable current-list table.
