server:
  port: 0
spring:
  session:
    jdbc:
      initialize-schema: never
  datasource:
    url: jdbc:h2:mem:tradeops;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
tradeops:
  bis:
    schedule-enabled: false
